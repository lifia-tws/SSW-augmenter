{% extends 'dvSSW2014Bundle::layout.html.twig' %}

{% block body -%}
    <h1>Query list</h1>

    <table class="records_list table table-hover">
        <thead>
            <tr>
                <th>Name</th>
                <th>Template</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('query_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.template }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('query_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('query_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
      <li>
        <button type="button" class="btn btn-success">
          <a href="{{ path('query_new') }}">
            Create a new entry
          </a>
        </button>
      </li>
    </ul>
    {% endblock %}
